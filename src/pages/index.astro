---
import { Image } from 'astro:assets'

import Layout from '../layouts/Layout.astro'
import Menu from '../components/Menu.astro'
import ryan from '../components/ryan.jpeg'

import { PostsSummary } from '../constants/PostsSummary.ts'
---

<Layout
	title="R.P. Trainor: Expert Laravel & JavaScript/TypeScript Engineer | Native & Web App Specialist"
	description="Experienced startup veteran with 8+ years in software engineering. Specializing in Laravel, JavaScript/TypeScript, and building apps from the ground up for startups and small businesses. Let’s create your next big thing."
>
	<div class="grid h-full w-full grid-cols-11 text-white">
		<div class="col-span-11 md:col-span-10">
			<div class="col-span-10 h-screen snap-y snap-mandatory overflow-y-auto md:col-span-11">
				<div
					class="grid h-screen snap-center grid-cols-phi grid-rows-phi gap-4 p-4 sm:gap-8 sm:p-8"
				>
					<div class="col-span-3 col-start-1 row-span-1 row-start-1 place-content-end text-right">
						<h1 class="text-3xl font-bold sm:text-4xl md:text-5xl lg:text-6xl">
							Expert Laravel &amp; Typescript Engineer
						</h1>
					</div>

					<div class="col-span-2 col-start-1 row-span-1 row-start-2 place-content-start text-right">
						<h2 class="text-balance text-2xl font-medium sm:text-3xl md:text-4xl">
							R&period;P&period; Trainor
						</h2>
					</div>
					<div class="col-span-1 col-start-3 row-span-1 row-start-2 place-content-start">
						<Image src={ryan} alt="Ryan Trainor" class="aspect-square h-auto w-24 rounded" />
					</div>
				</div>

				{
					PostsSummary.map((post, index) => {
						const isOdd = index % 2 === 0
						return (
							<div class="grid h-screen snap-center grid-cols-phi grid-rows-phi gap-4 p-4 sm:gap-8 sm:p-8">
								<div
									class={`${isOdd ? 'col-span-3 row-start-1' : 'col-span-1 row-start-2'} col-start-1 row-span-1 place-content-end text-right`}
								>
									<h2 class="text-balance text-2xl font-medium sm:text-3xl md:text-4xl">
										{post.date}
									</h2>
								</div>

								<div
									class={`${!isOdd ? 'col-span-3 row-start-1' : 'col-span-1 row-start-2'} col-start-1 row-span-1 place-content-start text-right`}
								>
									<h1 class="text-3xl font-bold sm:text-4xl md:text-5xl lg:text-6xl">
										{post.title}
									</h1>
								</div>
							</div>
						)
					})
				}
			</div>
		</div>

		<Menu />
	</div>
</Layout>
